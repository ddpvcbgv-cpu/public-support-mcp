# 🎯 v0.50 엔진 스펙 완전 구현 가이드

## 📋 개선 내용 요약

v0.50 엔진 스펙의 모든 요구사항을 충족하도록 서버를 업그레이드했습니다.

---

## ✅ 추가된 기능

### 1. **필수 문구 상수화** (`constants.py`)

엔진 스펙에서 요구하는 고정 문구들을 상수로 관리:
- ① 상황 요약 문구
- ② 분야 안내 필수 문구
- ⑤ 제도명 공개 경고
- ⑥ 확장 가능성 안내
- ⑦ 감정 안전 문장
- Onboarding 메시지

### 2. **새로운 도구 4개 추가**

#### 2.1. `collect_region_context` (지역 수집)
```python
# 사용자의 지역 정보를 부드럽게 수집
tools/region.py
```
- 지역 정보 요청 메시지 제공
- 지역 정보 저장 (state.region_hint)
- 지역 없이도 진행 가능

#### 2.2. `reveal_policy_name_if_triggered` (제도명 공개)
```python
# 3가지 트리거 감지 시 제도명 공개
tools/policy_trigger.py
```
**트리거 조건:**
- A: 카드 선택 ("이거", "1번", "더 알려줘")
- B: 제도명 질문 ("정확히 이름이 뭐예요?")
- C: 행동 의도 ("어디로 전화해요?", "신청하려면?")

**기본 동작:** 제도명 숨김 → 트리거 시에만 공개

#### 2.3. `suggest_followup_options` (확장 제안)
```python
# 추가 탐색 가능한 지원 분야 제안
tools/followup.py
```
- 현재 탐색한 분야 외 다른 분야 추천
- 키워드 기반 우선순위
- 긴급도에 따른 스마트 추천

#### 2.4. `orchestrate_full_response` (마스터 플로우)
```python
# ①~⑦ 단계를 자동으로 실행
tools/orchestrator.py
```

**자동 실행 단계:**
1. ① 상황 요약
2. ② 분야 안내 (필수 문구 포함)
3. ③ 혜택 카드 TOP 2~3
4. ④ 행동 단계 (오늘/내일/막히면)
5. 지역 수집 (필요 시)
6. ⑤ 제도명 공개 (트리거 시)
7. ⑥ 확장 가능성 안내
8. ⑦ 감정 안전 문장

**Onboarding 지원:**
- 최초 1회만 Onboarding 메시지 출력
- 이후에는 바로 ① 단계로 진입

---

## 🔧 도구 사용법

### 기존 방식 (수동)
```json
// 각 도구를 순서대로 호출
1. normalize_user_context
2. assess_urgency_level
3. expose_available_domains
4. rank_support_cards
5. generate_action_steps
6. generate_fallback_paths
7. compose_safe_response
```

### 새 방식 (자동) ⭐
```json
// 한 번의 호출로 전체 플로우 실행
{
  "tool": "orchestrate_full_response",
  "arguments": {
    "user_message": "부모님이랑 따로 사는 고등학생이에요, 경제적인 지원을 받을 수 있나요?",
    "skip_onboarding": false
  }
}
```

**응답 예시:**
```
안녕하세요.
저는 지금 상황을 함께 정리해드리는 공공 지원 내비게이터입니다.
...
(①~⑦ 단계 자동 실행)
```

---

## 📊 도구 목록 (전체 11개)

| 도구명 | 설명 | 엔진 스펙 매핑 |
|--------|------|----------------|
| `normalize_user_context` | 상황 정리 | ① 상황 요약 |
| `assess_urgency_level` | 긴급도 산정 | 내부 계산 |
| `expose_available_domains` | 분야 제안 | ② 분야 안내 |
| `rank_support_cards` | 혜택 카드 랭킹 | ③ 혜택 카드 |
| `generate_action_steps` | 행동 설계 | ④ 행동 단계 |
| `generate_fallback_paths` | 대안 경로 | ④ 막히면 |
| `compose_safe_response` | 감정 안전 | ⑦ 감정 안전 |
| `collect_region_context` | 지역 수집 | 지역 컨텍스트 |
| `reveal_policy_name_if_triggered` | 제도명 공개 | ⑤ 제도명 트리거 |
| `suggest_followup_options` | 확장 제안 | ⑥ 확장 가능성 |
| `orchestrate_full_response` ⭐ | 전체 플로우 | ①~⑦ 자동 실행 |

---

## 🎯 엔진 스펙 체크리스트

### ✅ 완료된 항목

- [x] ①~⑦ 응답 구조 구현
- [x] 필수 문구 고정 ("지금 상황을 기준으로 보면...")
- [x] 스마트 기본값 제안
- [x] 지역 컨텍스트 수집
- [x] 제도명 공개 트리거 (A/B/C)
- [x] 확장 가능성 안내 (항상 포함)
- [x] Onboarding 메시지 (최초 1회)
- [x] 세션 상태 관리 (shown_cards, region_hint 등)
- [x] 마스터 플로우 도구 (`orchestrate_full_response`)

### 🔄 추가 개선 가능 항목

- [ ] 카드별 제도명 매핑 DB 연동 (현재는 하드코딩)
- [ ] 지역별 연락처 자동 조회 (현재는 전국 공통)
- [ ] 사용자 프로파일 추론 강화 (NLP)

---

## 📖 사용 예시

### 예시 1: 마스터 플로우 사용 (권장)

**요청:**
```json
POST /mcp/call
{
  "tool": "orchestrate_full_response",
  "arguments": {
    "user_message": "서울에서 취업을 3년 동안 못하고 있는데 취준에 도움을 받을 수 있나요?",
    "skip_onboarding": true
  },
  "session_id": "abc123"
}
```

**응답:**
```
지금 말씀해주신 상황을 정리해보면, 서울에서 취업 준비를 3년 정도 하셨고, 
지금은 도움을 받을 수 있는 방법을 찾고 계신 것으로 이해했어요.

지금 상황을 기준으로 보면, 한 가지가 아니라 몇 갈래가 열려 있습니다.

  • 고용·교육
  • 생활 유지
  • 주거·월세

제가 보기에는 지금은 고용·교육 / 생활 유지 / 주거·월세 이 세 갈래가 우선이에요.
원하시면 제가 먼저 하나만 정리해드릴까요?

지금 상황을 기준으로 보면, 고용·교육 분야에서 열려 있는 선택지를 정리해봤어요.

【소득 회복 탐색】

이게 뭐냐면:
단기·파트타임·교육 연계를 통해 소득 공백을 메우는 상담을 받을 수 있습니다.

왜 지금 맞냐면:
취업 준비 기간이 길어질수록, 소득과 경험을 동시에 확보하는 방식이 도움이 되는 경우가 많아서요.

지금 하실 수 있는 말:
  → "소득이 줄었어요. 단기 일자리나 교육 연계 가능성만 알아보고 싶어요."

어디로:
고용센터 1350에 전화하거나 워크넷(www.work.go.kr), 지역 일자리센터를 방문하세요.

방법:
1) 구직등록 후 실업급여 해당 시 신청
2) 직업훈련 상담
3) 취업성공패키지 신청
4) 단기 일자리 정보 확인

막히면:
전화 연결이 어려우면 워크넷에서 온라인 상담을 먼저 시도해보세요.

---

【지금 바로 할 수 있는 행동】

오늘: 현재 상황을 한 문장으로 정리해보세요. 예) "고용·교육 관련 지원 필요".
      이 문장을 가지고 전화·상담을 시작하면 부담이 줄어요.

내일: 고용센터나 지역 일자리센터에 "구직 상담 받고 싶다"고 문의해보세요.
      소득·경험 상황을 간단히 말씀드리면 됩니다.

막히면: 전화가 어려우면 온라인 채팅 상담이나 워크넷 게시판을 이용해보세요.

그리고 참고로, 말씀해주신 상황을 기준으로 보면
지금 이야기한 영역 외에도 함께 살펴볼 수 있는 지원의 갈래가 조금 더 있습니다.
원하시면 지금 선택하신 방향을 기준으로 하나씩 이어서 정리해드릴게요.
부담 없이 말씀 주셔도 괜찮습니다.

예를 들면:
  • 생활 유지: 취업 준비 기간을 버틸 수 있는 생활비 지원도 함께 확인해볼 여지가 있어요.
  • 주거·월세: 주거 안정이 우선될 때, 다른 지원도 연결이 더 쉬워지는 경우가 많아요.

---

지금 이 단계에서 취업을 오래 못 했다는 사실이 불리한 자격이 되는 건 아닙니다.
도움을 받을 수 있는지 확인하고, 어디서부터 다시 시작할지 고르고 계신 지금 이 과정 자체가
이미 한 걸음입니다. 속도와 방향은 지금 질문하신 분이 정하셔도 괜찮아요.
```

### 예시 2: 제도명 트리거

**사용자:** "첫 번째 카드 더 자세히 알려줘"

**시스템 감지:**
- Trigger A (카드 선택) 감지
- state.handoff_intent = "card_selected"

**응답:**
```
⚠️ 말씀드리기 전에, 제도는 지역과 시점에 따라 적용이 달라질 수 있어요.

[소득 회복 탐색]은(는) 보통 다음과 같은 제도와 연결되는 경우가 많습니다:
  → 취업성공패키지, 국민취업지원제도
```

---

## 🚀 배포 방법

### 로컬 테스트
```bash
cd /c/public-support-mcp
python -m uvicorn main:app --reload --host 0.0.0.0 --port 3100
```

### Git 푸시
```bash
git add constants.py tools/region.py tools/policy_trigger.py tools/followup.py tools/orchestrator.py main.py v0.50_UPGRADE_GUIDE.md
git commit -m "feat: v0.50 엔진 스펙 완전 구현

- 필수 문구 상수화 (constants.py)
- 지역 수집 도구 추가 (tools/region.py)
- 제도명 공개 트리거 추가 (tools/policy_trigger.py)
- 확장 제안 도구 추가 (tools/followup.py)
- 마스터 플로우 도구 추가 (tools/orchestrator.py)
- Onboarding 메시지 지원
- ①~⑦ 응답 구조 자동화"
git push origin main
```

### Render 재배포
- Render 대시보드에서 자동 배포됨 (약 2~3분 소요)
- 또는 수동 배포: **Deploy → Deploy latest commit**

---

## 📞 테스트 체크리스트

### 1. Onboarding 테스트
```json
{
  "tool": "orchestrate_full_response",
  "arguments": {
    "user_message": "안녕하세요",
    "skip_onboarding": false
  }
}
```
**기대 결과:** Onboarding 메시지 출력

### 2. 전체 플로우 테스트
```json
{
  "tool": "orchestrate_full_response",
  "arguments": {
    "user_message": "부모님이랑 따로 사는 고등학생이에요, 경제적인 지원을 받을 수 있나요?",
    "skip_onboarding": true
  }
}
```
**기대 결과:** ①~⑦ 단계 모두 출력

### 3. 제도명 트리거 테스트
```json
{
  "tool": "reveal_policy_name_if_triggered",
  "arguments": {
    "message": "첫 번째 카드 더 알려줘"
  }
}
```
**기대 결과:** 제도명 공개

### 4. 지역 수집 테스트
```json
{
  "tool": "collect_region_context",
  "arguments": {}
}
```
**기대 결과:** 지역 정보 요청 메시지

---

## 🎓 철학 (v0.50)

> 이 시스템은 **"혜택을 판정해주는 도구"**가 아니라  
> **"사람이 스스로 선택할 수 있게 옆에서 정리해주는 동행자"**다.

---

## 📚 참고 문서

- `constants.py`: 필수 문구 및 상수
- `tools/orchestrator.py`: 마스터 플로우 로직
- `HOW_TO_CREATE_NEW_MCP.md`: 새 MCP 서버 생성 가이드
- `MCP_TEMPLATE_GUIDE.md`: MCP 템플릿 사용법

---

**업그레이드 완료 ✅**  
v0.50 엔진 스펙의 모든 요구사항을 100% 충족합니다.

